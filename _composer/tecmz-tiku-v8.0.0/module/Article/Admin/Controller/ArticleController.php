<?php
/**
 * ------------------------ 
 *  版权所有  www.tecmz.com
 *  商业版本请购买正版授权使用
 * ------------------------
*/ namespace Module\Article\Admin\Controller; use Illuminate\Routing\Controller; use ModStart\Admin\Concern\HasAdminQuickCRUD; use ModStart\Admin\Layout\AdminCRUDBuilder; use ModStart\Field\AbstractField; use ModStart\Field\Type\FieldRenderMode; use ModStart\Form\Form; use ModStart\Grid\GridFilter; use ModStart\Support\Concern\HasFields; use ModStart\Widget\TextLink; use Module\Article\Type\ArticlePosition; use Module\Article\Util\ArticleUtil; class ArticleController extends Controller { use HasAdminQuickCRUD; protected function crud(AdminCRUDBuilder $cv5kq) { $cv5kq->init('article')->field(function ($cv5kq) { $cv5kq->id('id', 'ID'); $cv5kq->select('position', '位置')->optionType(ArticlePosition::class); $cv5kq->text('title', '名称')->hookRendering(function (AbstractField $QGYmu, $REa1I, $x1Lvn) { switch ($QGYmu->renderMode()) { case FieldRenderMode::GRID: case FieldRenderMode::DETAIL: return TextLink::primary(htmlspecialchars($REa1I->title), ArticleUtil::url($REa1I->toArray()), 'target="_blank"'); } }); $cv5kq->text('alias', '别名')->help('默认留空，可通过链接 /article/别名 访问'); $cv5kq->richHtml('content', '内容')->listable(false); $cv5kq->display('created_at', L('Created At'))->listable(false); $cv5kq->display('updated_at', L('Updated At'))->listable(false); })->gridFilter(function (GridFilter $qM7YH) { $qM7YH->eq('position', '位置')->select(ArticlePosition::class); $qM7YH->like('title', L('Title')); })->title('单页文章')->hookSaved(function (Form $Yfg4L) { ArticleUtil::clearCache(); }); } }