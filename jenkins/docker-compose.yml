services:
  jenkins:
    image: jenkins/jenkins:latest
    container_name: jenkins
    ports:
      - 8081:8080
      - 50000:50000
    # volumes:
    restart: unless-stopped
    network_mode: bridge
